# TODO

## Follow-ups
- [대형객체 chunking 통합] v0.0.583.0(simple PUT 단일경로) + v0.0.584.0(빈객체·internal 면제 제거 → 모든 simple PUT이 size·bucket 무관 단일 청킹, write+read). 잔여: (a) 멀티파트-complete/EC-rewrap 단일화 여부(별 operation), (b) EC-memory·single-local-spooled 브랜치 완전제거(멀티파트/rewrap/unwired-test가 사용, shardGroup 보편화 필요), (c) [P3-perf] 작은객체 디스크 spool(메모리-티어 spool, perf deprioritized), (d) [pre-existing, S2무관] e2e "S3 versioning SingleNode PutGetByVersionID" 404 NoSuchKey(master에서도 실패, 별 버그).


- [P3] EC 샤드용 orphan scrubber 미구현. `OrphanWalkable`(internal/scrubber/orphan.go) 인터페이스와 `BackgroundScrubber.orphanSweep`(scrubber.go:710 type-assert) 프레임워크는 있으나, `DistributedBackend`가 `WalkOrphanShards`/`DeleteOrphanDir`를 구현하지 않아 production에서 EC 샤드 orphan sweep이 **전혀 실행되지 않음**. 결과: 어떤 경로로든 누출된 EC 샤드(full-object `<key>/<versionID>` + chunked segment `<key>/segments/<blobID>`)는 영구 잔존. 구현 시 두 경로의 디스크 레이아웃을 모두 스캔하고 `ScanObjects` known-set과 대조해야 함(segment walker는 `<key>_segments/<blobID>`만 스캔하므로 별도). NOTE: `ScanObjects`는 이제 FSM `lat:` + quorum-meta 머지(2026-06-15 v0.0.578.0)라 regular-PUT 객체까지 known-set에 포함됨 → orphan sweep의 false-positive 위험이 그만큼 감소(이전엔 regular-PUT 객체가 known-set에 없어 위험). codex code-gate(2026-06-14) 발견. 이게 있으면 아래 phantom-commit 잔여 누출도 자동 회수됨.

- [WAL 제거 에픽 — 완료] S0(quorum-meta scrubber 커버리지)=v0.0.578.0, S1(redundant large 샤드 metadata-only WAL 레코드 drop)=v0.0.579.0, S2(작은 샤드+no-redundancy large 샤드를 file fsync + dir-chain fsync로 전환, OpShardPut 레코드 drop, DBV 불변식)=v0.0.580.0, S3(shard-packing 비활성+전체 제거)=v0.0.581.0, **S4(production WAL teardown)=v0.0.582.0 완료 → 에픽 종료**. S4: boot의 `datawal.Open`/`WithDataWAL`/`WithDataWALRepairSink`/`RecoverDataWAL` replay 제거, ShardService WAL 표면(`DataWALAppender`/`AppendShardMetadataBatch`/`RecoverDataWAL`/머티리얼라이저)·datawal startup-repair 서브시스템·6개 `grainfs_datawal_startup_repair_*` 메트릭 삭제. stream write nil-WAL guard 제거(64MiB cap 유지). **migration: boot=NO WAL replay** — pre-S2에서 WAL에만 있던(미fsync) 샤드는 업그레이드 시 손실(production 배포 없음 전제, 사용자 2026-06-15 확정). `datawal` Go 패키지 + LocalBackend fixture(NamespaceNode) 유지(독립 인스턴스). **`--direct-io` 플래그 제거(breaking)**: directIO write 경로는 삭제된 WAL-replay materializer 전용이었고 production writeEncryptedShardFile은 S2이후 directIO 미사용 → 이미 prod no-op이었음(사용자 2026-06-15 확정, dead feature 전체 제거). packing 부활 시 durable pack-index는 deferred follow-up.
- [P3] WAL 제거 잔여 cosmetic: `walPayloadInlineThreshold`/`maxRawShardPayloadForWAL`는 이제 misnomer(WAL 없음 → `largeShardFsyncThreshold`/`maxRawShardPayload`로 rename 가능), `readShardPayload` over-cap 에러 문자열 "data WAL shard payload too large"도 stale. `syncDirChain`은 같은 dir 반복 쓰기 시 이미 durable한 ancestor 재-fsync(신규 생성 레벨만 추적하면 최적화 가능). S5(segment/object WAL ops)는 LocalBackend test-fixture 전용·production caller 0(ADR-0015)이라 production 목표 무관. 로드맵/설계는 spec(docs/superpowers/specs/2026-06-14-wal-removal-design.md, git-untracked).
- [P3] phantom-commit 잔여 누출 (위 EC orphan scrubber로 해소 예정). multipart-complete propose가 `ErrProposeTimeout`/`context.Canceled`로 실패하면 shard를 보존(commit됐을 수 있는 객체 파괴 방지, `shardCleanupSafeOnProposeError`)하는데, 그 entry가 진짜로 commit되지 않은 rare² 경우엔 shard가 누출됨(현재 EC orphan scrubber 부재로 회수 안 됨). lost-forward-reply(leader commit + 응답 유실, timeout/cancel 아님) tail도 잔존 — 이 경우 eager-delete가 commit된 객체를 지울 수 있으나 매우 드묾. 완전 해소엔 propose 계약이 "committed vs not" 구별을 노출하거나 EC orphan scrubber가 필요.
