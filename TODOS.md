# TODO

## Follow-ups
- [대형객체 chunking 통합 — 잔여] (a) 멀티파트-complete/EC-rewrap 단일화 여부(별 operation), (b) EC-memory·single-local-spooled 브랜치 완전제거(멀티파트/rewrap/unwired-test가 사용, shardGroup 보편화 필요), (c) [P3-perf] 작은객체 디스크 spool(메모리-티어 spool, perf deprioritized).

- [feature·안전-크리티컬·보류] EC full-object orphan shard scrubber 미구현. `OrphanWalkable`(internal/scrubber/orphan.go)+`orphanSweep`(scrubber.go:710 type-assert) 프레임워크는 있으나 `DistributedBackend`가 `WalkOrphanShards`/`DeleteOrphanDir`를 미구현 → production에서 EC full-object 샤드 orphan sweep이 **전혀 안 돎**. 누출 샤드(`<dataDir>/<bucket>/<key>/<versionID>/shard_N`) 영구 잔존. 2026-06-15 codex plan-gate **BLOCK** → P3가 아니라 segment-GC 안전장치 전체를 샤드용으로 복제해야 하는 feature급 슬라이스로 판명, **보류**(사용자: Item 3 먼저). 핵심 landmine: ①ScanObjects가 **latest-only**(FSM lat: + quorum-meta latest)라 versioning 버킷 옛버전 샤드를 orphan 오판=데이터손실 ②ShardService dataDirs는 **노드-레벨 공유**(모든 그룹 commingle, group식별자 無)인데 sweep은 group-0 distBackend known(자기그룹만)으로 전체 walk=멀티그룹 cross-group 손실 ③shard_N을 full-object 외 segments/coalesced/repair-rewrap 경로도 씀=판별자 위험 ④snapshot-pin/caught-up/write-lease 부재. spec=`docs/superpowers/specs/2026-06-15-ec-orphan-shard-scrubber.md`(root worktree, git-untracked) + 7 must-fix. 재개 시 spec 갱신→plan-gate re-gate→TDD. 구현되면 아래 phantom-commit 잔여 누출도 자동 회수.

- [P3-perf] WAL 제거 잔여 — `syncDirChain` 최적화: 같은 dir 반복 쓰기 시 이미 durable한 ancestor를 재-fsync함(신규 생성 레벨만 추적하면 최적화 가능). behavior-neutral 아님(perf 변경)이라 cosmetic rename과 분리. S5(segment/object WAL ops)는 LocalBackend test-fixture 전용·production caller 0(ADR-0015)이라 production 목표 무관. 로드맵/설계는 spec(docs/superpowers/specs/2026-06-14-wal-removal-design.md, git-untracked).
- [P3] phantom-commit 잔여 누출 (위 EC orphan scrubber로 해소 예정). multipart-complete propose가 `ErrProposeTimeout`/`context.Canceled`로 실패하면 shard를 보존(commit됐을 수 있는 객체 파괴 방지, `shardCleanupSafeOnProposeError`)하는데, 그 entry가 진짜로 commit되지 않은 rare² 경우엔 shard가 누출됨(현재 EC orphan scrubber 부재로 회수 안 됨). lost-forward-reply(leader commit + 응답 유실, timeout/cancel 아님) tail도 잔존 — 이 경우 eager-delete가 commit된 객체를 지울 수 있으나 매우 드묾. 완전 해소엔 propose 계약이 "committed vs not" 구별을 노출하거나 EC orphan scrubber가 필요.
