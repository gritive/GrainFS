# Concepts Overview

GrainFS exposes object, file, block, and table/catalog interfaces from one storage layer. The main concept is not “five separate servers”; it is one namespace with multiple protocol views.

Start with the quickstart when you want to run it. Read this section when you need the product model before operating it.
