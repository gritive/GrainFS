# Compatibility Claims

A customer-facing `Supported` claim requires e2e, conformance, or real client integration evidence. Unit tests alone are not enough.

When updating a compatibility matrix, include the test evidence or mark the feature partial/unsupported.
