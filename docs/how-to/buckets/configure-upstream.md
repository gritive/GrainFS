# How to Configure a Bucket Upstream

Bucket upstream configuration is used for migration and cutover workflows.

Use the `grainfs bucket upstream` command family and verify behavior with the migration guide before changing production traffic.
