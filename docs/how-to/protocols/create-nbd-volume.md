# How to Create an NBD Volume

Use `grainfs volume` commands to create and inspect volumes. Linux NBD client tooling is required to attach the device.

Check [NBD compatibility](../../reference/nbd-compatibility.md) before production use.
