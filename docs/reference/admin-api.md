# Admin API Reference

Mutating administration uses the local admin Unix domain socket by default. CLI commands wrap this surface for most operators.

Document customer-supported admin operations here, not internal handler details.
