# Config Reference

This page tracks runtime configuration keys and whether they can be hot-applied.

Use this as the public reference for operator-visible configuration. Internal planning notes and ADR drafts are intentionally excluded from this documentation set.
