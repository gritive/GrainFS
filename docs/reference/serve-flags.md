# Serve Flags

`grainfs serve` starts the server. Use `grainfs serve --help` from the built binary for the exact flag list for your version.

Common flags include data path, listener ports, protocol toggles, cluster settings, and operational endpoints. Keep production values in config management rather than ad hoc shell history.
