# Zero-copy 최적화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** EC 핫패스 `append` 오버헤드 제거 + VFS `grainFile`에 스트리밍 모드 추가로 NFS 순차 읽기 시 `io.ReadAll` 버퍼링 제거.

**Architecture:** Task 1은 `internal/cluster/ec.go`의 ECSplit에서 double-append를 copy 2회로 교체한다. Task 2–4는 `internal/vfs/vfs.go`의 `grainFile`에 `rc io.ReadCloser` 필드를 추가하고, 읽기 전용 Open 시 `loadExisting()` 없이 rc를 저장해 스트리밍한다. Seek/ReadAt 호출 시 `loadExisting()` fallback으로 전환한다. 두 Task는 서로 다른 패키지이므로 병렬 작업 가능.

**Tech Stack:** Go stdlib (`io`, `bytes`, `os`), `storage.Backend.GetObject(bucket, key) (io.ReadCloser, *Object, error)`, `go test -bench`, `make test`

---

## 변경 파일 목록

| 파일 | 역할 |
|------|------|
| `internal/cluster/ec.go` | ECSplit double-append → copy 교체 |
| `internal/cluster/ec_test.go` | BenchmarkECSplit 추가 |
| `internal/vfs/vfs.go` | grainFile 스트리밍 모드 구현 |
| `internal/vfs/vfs_test.go` | 스트리밍 유닛 테스트 3개 추가 |

---

## Task 1: ECSplit append → copy 최적화

**Files:**
- Modify: `internal/cluster/ec.go:119-123`
- Modify: `internal/cluster/ec_test.go` (BenchmarkECSplit 추가)

### 배경

`ECSplit`의 shard 조합 루프에서 `make([]byte, 0, cap)` + `append` 2회를 `make([]byte, size)` + `copy` 2회로 교체한다. capacity는 이미 정확히 예약되어 있으므로 append 호출 자체의 오버헤드(slice header 갱신, 경계 체크)를 제거하는 것이 목적이다.

---

- [ ] **Step 1: BenchmarkECSplit 작성 (베이스라인 측정)**

`internal/cluster/ec_test.go` 끝에 추가:

```go
func BenchmarkECSplit(b *testing.B) {
	cfg := ECConfig{DataShards: 4, ParityShards: 2}
	data := make([]byte, 1<<20) // 1 MiB
	_, err := rand.Read(data)
	require.NoError(b, err)
	b.ResetTimer()
	b.ReportAllocs()
	for i := 0; i < b.N; i++ {
		_, _ = ECSplit(cfg, data)
	}
}
```

- [ ] **Step 2: 현재 벤치마크 실행 (변경 전 baseline 기록)**

```bash
cd /Users/whitekid/work/gritive/grains
go test ./internal/cluster/ -run='^$' -bench=BenchmarkECSplit -benchmem -count=3
```

예상 출력 (참고용):
```
BenchmarkECSplit-N   NNN ns/op   XXXXXX B/op   N allocs/op
```

기록해 두고 Step 7과 비교한다.

- [ ] **Step 3: ECSplit 루프 수정**

`internal/cluster/ec.go`에서 아래 부분을 찾아 교체한다.

변경 전 (`ec.go` 약 119-123번 줄, `for i, s := range shards {` 블록 내부):
```go
		payload := make([]byte, 0, shardHeaderSize+len(s))
		payload = append(payload, header[:]...)
		payload = append(payload, s...)
		out[i] = payload
```

변경 후:
```go
		payload := make([]byte, shardHeaderSize+len(s))
		copy(payload, header[:])
		copy(payload[shardHeaderSize:], s)
		out[i] = payload
```

- [ ] **Step 4: 기존 EC 유닛 테스트 통과 확인**

```bash
go test ./internal/cluster/ -run=TestECSplit -v
```

예상: 모든 TestECSplit_* PASS.

- [ ] **Step 5: 전체 cluster 패키지 테스트 통과 확인**

```bash
go test ./internal/cluster/ -timeout=60s
```

예상: PASS, no failures.

- [ ] **Step 6: 변경 후 벤치마크 실행**

```bash
go test ./internal/cluster/ -run='^$' -bench=BenchmarkECSplit -benchmem -count=3
```

allocs/op는 Step 2와 동일(1회)이어야 하고, ns/op는 동일하거나 감소해야 한다.

- [ ] **Step 7: 커밋**

```bash
gofmt -w internal/cluster/ec.go internal/cluster/ec_test.go
git add internal/cluster/ec.go internal/cluster/ec_test.go
git commit -m "perf(cluster): ECSplit double-append → copy (zero-copy Phase 19)"
```

---

## Task 2: VFS grainFile 스트리밍 테스트 작성 (TDD)

**Files:**
- Modify: `internal/vfs/vfs_test.go`

### 배경

`grainFile`에 `rc io.ReadCloser` 필드를 추가하기 전에 실패하는 테스트를 먼저 작성한다. 테스트는 `package vfs`(internal) 이므로 unexported 필드에 직접 접근 가능하다.

---

- [ ] **Step 1: 스트리밍 테스트 3개 작성**

`internal/vfs/vfs_test.go` 끝에 추가:

```go
// TestGrainFileStreamRead: 읽기 전용 Open은 스트리밍 모드(rc != nil, buf == nil)여야 하며
// io.ReadAll로 전체 내용을 올바르게 반환해야 한다.
func TestGrainFileStreamRead(t *testing.T) {
	fs := setupFS(t)

	content := []byte("hello streaming world")
	f, err := fs.Create("stream.txt")
	require.NoError(t, err)
	_, err = f.Write(content)
	require.NoError(t, err)
	require.NoError(t, f.Close())

	// 읽기 전용으로 열면 스트리밍 모드여야 함
	f2, err := fs.Open("stream.txt")
	require.NoError(t, err)

	gf := f2.(*grainFile)
	assert.NotNil(t, gf.rc, "읽기 전용 Open은 rc가 설정되어야 함 (스트리밍 모드)")
	assert.Nil(t, gf.buf, "스트리밍 모드에서 buf는 nil이어야 함")

	got, err := io.ReadAll(f2)
	require.NoError(t, err)
	assert.Equal(t, content, got)
	require.NoError(t, f2.Close())
}

// TestGrainFileSeekFallback: 스트리밍 모드 중 Seek 호출 시 loadExisting()으로 fallback해
// buf 모드로 전환되어야 하며, 이후 읽기는 파일 전체를 정확히 반환해야 한다.
func TestGrainFileSeekFallback(t *testing.T) {
	fs := setupFS(t)

	content := []byte("0123456789")
	f, err := fs.Create("seek.txt")
	require.NoError(t, err)
	_, err = f.Write(content)
	require.NoError(t, err)
	require.NoError(t, f.Close())

	f2, err := fs.Open("seek.txt")
	require.NoError(t, err)

	// 앞 5바이트 스트리밍 읽기
	buf := make([]byte, 5)
	n, err := f2.Read(buf)
	require.NoError(t, err)
	assert.Equal(t, 5, n)
	assert.Equal(t, []byte("01234"), buf)

	// Seek(0, SeekStart) → rc fallback 트리거
	pos, err := f2.Seek(0, io.SeekStart)
	require.NoError(t, err)
	assert.Equal(t, int64(0), pos)

	gf := f2.(*grainFile)
	assert.Nil(t, gf.rc, "Seek 후 rc는 nil이어야 함 (buf 모드로 전환)")
	assert.NotNil(t, gf.buf, "Seek 후 buf가 로드되어야 함")

	// 처음부터 전체 읽기
	got, err := io.ReadAll(f2)
	require.NoError(t, err)
	assert.Equal(t, content, got)
	require.NoError(t, f2.Close())
}

// TestGrainFileCloseWithUnconsumedRC: Read 없이 Close하면 rc.Close()가 호출되어
// 리소스 릭이 발생하지 않아야 하며, 이후 파일 재접근이 가능해야 한다.
func TestGrainFileCloseWithUnconsumedRC(t *testing.T) {
	fs := setupFS(t)

	f, err := fs.Create("no-read.txt")
	require.NoError(t, err)
	_, err = f.Write([]byte("data"))
	require.NoError(t, err)
	require.NoError(t, f.Close())

	// 열고 읽지 않은 채 닫기
	f2, err := fs.Open("no-read.txt")
	require.NoError(t, err)

	gf := f2.(*grainFile)
	assert.NotNil(t, gf.rc, "Open 직후 스트리밍 모드여야 함")

	err = f2.Close()
	require.NoError(t, err)
	assert.Nil(t, gf.rc, "Close 후 rc는 nil이어야 함")

	// 파일이 손상되지 않았는지 재확인
	f3, err := fs.Open("no-read.txt")
	require.NoError(t, err)
	got, err := io.ReadAll(f3)
	require.NoError(t, err)
	require.NoError(t, f3.Close())
	assert.Equal(t, []byte("data"), got)
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
go test ./internal/vfs/ -run='TestGrainFileStream|TestGrainFileSeek|TestGrainFileCloseWithUnconsumed' -v
```

예상: 3개 모두 FAIL — `gf.rc`가 nil이므로 `assert.NotNil(t, gf.rc, ...)` 실패.

---

## Task 3: VFS grainFile 스트리밍 구현

**Files:**
- Modify: `internal/vfs/vfs.go`

### 변경 내용 개요

1. `grainFile` 구조체에 `rc io.ReadCloser` 추가
2. `OpenFile`: 읽기 전용(`flag == os.O_RDONLY`)에서 `loadExisting()` 대신 rc 저장
3. `loadExisting()`: rc가 있으면 닫고 fresh GetObject로 전체 로드
4. `Read()`: rc != nil이면 rc.Read() 직접 호출
5. `Seek()`: rc != nil이면 loadExisting() fallback
6. `ReadAt()`: rc != nil이면 loadExisting() fallback
7. `Close()`: rc != nil이면 rc.Close()

---

- [ ] **Step 1: grainFile 구조체에 rc 필드 추가**

`internal/vfs/vfs.go`의 `grainFile` 구조체 (약 425번 줄):

변경 전:
```go
type grainFile struct {
	fs     *GrainVFS
	path   string
	name   string
	flag   int
	perm   os.FileMode
	buf    *bytes.Buffer
	pos    int64
	closed bool
}
```

변경 후:
```go
type grainFile struct {
	fs     *GrainVFS
	path   string
	name   string
	flag   int
	perm   os.FileMode
	buf    *bytes.Buffer
	pos    int64
	closed bool
	rc     io.ReadCloser // 스트리밍 모드: 읽기 전용 Open 시 설정, Seek/ReadAt/Close 시 해제
}
```

- [ ] **Step 2: loadExisting() 수정 — rc 있을 때 close 후 fresh GetObject**

`internal/vfs/vfs.go`의 `loadExisting()` (약 436번 줄):

변경 전:
```go
func (f *grainFile) loadExisting() error {
	rc, _, err := f.fs.backend.GetObject(f.fs.bucket, f.path)
	if err != nil {
		return os.ErrNotExist
	}
	data, err := io.ReadAll(rc)
	rc.Close()
	if err != nil {
		return fmt.Errorf("read file: %w", err)
	}
	f.buf = bytes.NewBuffer(data)
	return nil
}
```

변경 후:
```go
func (f *grainFile) loadExisting() error {
	if f.rc != nil {
		f.rc.Close()
		f.rc = nil
	}
	rc, _, err := f.fs.backend.GetObject(f.fs.bucket, f.path)
	if err != nil {
		return os.ErrNotExist
	}
	data, err := io.ReadAll(rc)
	rc.Close()
	if err != nil {
		return fmt.Errorf("read file: %w", err)
	}
	f.buf = bytes.NewBuffer(data)
	return nil
}
```

- [ ] **Step 3: OpenFile 수정 — 읽기 전용 경로에서 스트리밍 모드 사용**

`internal/vfs/vfs.go`의 `OpenFile()` 끝부분 (약 159-163번 줄):

변경 전:
```go
	// Must exist
	if err := f.loadExisting(); err != nil {
		return nil, err
	}
	return f, nil
```

변경 후:
```go
	// Must exist
	if flag == os.O_RDONLY {
		// 읽기 전용: rc를 저장해 스트리밍 모드로 진입 (io.ReadAll 버퍼링 없음)
		rc, _, err := fs.backend.GetObject(fs.bucket, fp)
		if err != nil {
			return nil, os.ErrNotExist
		}
		f.rc = rc
		return f, nil
	}
	if err := f.loadExisting(); err != nil {
		return nil, err
	}
	return f, nil
```

- [ ] **Step 4: Read() 수정 — rc 모드에서 직접 스트리밍**

`internal/vfs/vfs.go`의 `Read()` (약 459번 줄):

변경 전:
```go
func (f *grainFile) Read(p []byte) (int, error) {
	if f.buf == nil {
		return 0, io.EOF
	}
	data := f.buf.Bytes()
	if f.pos >= int64(len(data)) {
		return 0, io.EOF
	}
	n := copy(p, data[f.pos:])
	f.pos += int64(n)
	return n, nil
}
```

변경 후:
```go
func (f *grainFile) Read(p []byte) (int, error) {
	if f.rc != nil {
		n, err := f.rc.Read(p)
		f.pos += int64(n)
		return n, err
	}
	if f.buf == nil {
		return 0, io.EOF
	}
	data := f.buf.Bytes()
	if f.pos >= int64(len(data)) {
		return 0, io.EOF
	}
	n := copy(p, data[f.pos:])
	f.pos += int64(n)
	return n, nil
}
```

- [ ] **Step 5: Seek() 수정 — rc 모드에서 loadExisting() fallback**

`internal/vfs/vfs.go`의 `Seek()` (약 487번 줄):

변경 전:
```go
func (f *grainFile) Seek(offset int64, whence int) (int64, error) {
	size := int64(0)
	if f.buf != nil {
		size = int64(f.buf.Len())
	}

	switch whence {
	case io.SeekStart:
		f.pos = offset
	case io.SeekCurrent:
		f.pos += offset
	case io.SeekEnd:
		f.pos = size + offset
	}

	if f.pos < 0 {
		f.pos = 0
	}
	return f.pos, nil
}
```

변경 후:
```go
func (f *grainFile) Seek(offset int64, whence int) (int64, error) {
	if f.rc != nil {
		if err := f.loadExisting(); err != nil {
			return 0, err
		}
	}
	size := int64(0)
	if f.buf != nil {
		size = int64(f.buf.Len())
	}

	switch whence {
	case io.SeekStart:
		f.pos = offset
	case io.SeekCurrent:
		f.pos += offset
	case io.SeekEnd:
		f.pos = size + offset
	}

	if f.pos < 0 {
		f.pos = 0
	}
	return f.pos, nil
}
```

- [ ] **Step 6: ReadAt() 수정 — rc 모드에서 loadExisting() fallback**

`internal/vfs/vfs.go`의 `ReadAt()` (약 472번 줄):

변경 전:
```go
func (f *grainFile) ReadAt(p []byte, off int64) (int, error) {
	if f.buf == nil {
		return 0, io.EOF
	}
	data := f.buf.Bytes()
	if off >= int64(len(data)) {
		return 0, io.EOF
	}
	n := copy(p, data[off:])
	if n < len(p) {
		return n, io.EOF
	}
	return n, nil
}
```

변경 후:
```go
func (f *grainFile) ReadAt(p []byte, off int64) (int, error) {
	if f.rc != nil {
		if err := f.loadExisting(); err != nil {
			return 0, err
		}
	}
	if f.buf == nil {
		return 0, io.EOF
	}
	data := f.buf.Bytes()
	if off >= int64(len(data)) {
		return 0, io.EOF
	}
	n := copy(p, data[off:])
	if n < len(p) {
		return n, io.EOF
	}
	return n, nil
}
```

- [ ] **Step 7: Close() 수정 — rc 미소비 시 Close**

`internal/vfs/vfs.go`의 `Close()` (약 508번 줄):

변경 전:
```go
func (f *grainFile) Close() error {
	if f.closed {
		return nil
	}
	f.closed = true

	// Flush writes to storage
	if f.flag&(os.O_WRONLY|os.O_RDWR|os.O_CREATE|os.O_TRUNC) != 0 && f.buf != nil {
		data := f.buf.Bytes()
		_, err := f.fs.backend.PutObject(f.fs.bucket, f.path,
			bytes.NewReader(data), "application/octet-stream")
		if err != nil {
			return err
		}
		// Invalidate caches after write
		f.fs.invalidateStatCache(f.path)
		f.fs.invalidateParentDirCache(f.path)
		return nil
	}
	return nil
}
```

변경 후:
```go
func (f *grainFile) Close() error {
	if f.closed {
		return nil
	}
	f.closed = true

	if f.rc != nil {
		f.rc.Close()
		f.rc = nil
	}

	// Flush writes to storage
	if f.flag&(os.O_WRONLY|os.O_RDWR|os.O_CREATE|os.O_TRUNC) != 0 && f.buf != nil {
		data := f.buf.Bytes()
		_, err := f.fs.backend.PutObject(f.fs.bucket, f.path,
			bytes.NewReader(data), "application/octet-stream")
		if err != nil {
			return err
		}
		// Invalidate caches after write
		f.fs.invalidateStatCache(f.path)
		f.fs.invalidateParentDirCache(f.path)
		return nil
	}
	return nil
}
```

- [ ] **Step 8: gofmt 적용**

```bash
gofmt -w internal/vfs/vfs.go
```

- [ ] **Step 9: 스트리밍 테스트 3개 통과 확인**

```bash
go test ./internal/vfs/ -run='TestGrainFileStream|TestGrainFileSeek|TestGrainFileCloseWithUnconsumed' -v
```

예상:
```
--- PASS: TestGrainFileStreamRead
--- PASS: TestGrainFileSeekFallback
--- PASS: TestGrainFileCloseWithUnconsumedRC
```

- [ ] **Step 10: 전체 VFS 테스트 통과 확인 (회귀 없음)**

```bash
go test ./internal/vfs/ -timeout=60s
```

예상: PASS, no failures. 특히 TestSeek, TestReadAt, TestRename 등 기존 테스트 모두 통과.

- [ ] **Step 11: 커밋**

```bash
gofmt -w internal/vfs/vfs_test.go
git add internal/vfs/vfs.go internal/vfs/vfs_test.go
git commit -m "perf(vfs): grainFile 스트리밍 모드 — 읽기 전용 Open 시 io.ReadAll 제거 (zero-copy Phase 19)"
```

---

## Task 4: 전체 빌드 및 E2E 확인

**Files:**
- 변경 없음 (검증만)

---

- [ ] **Step 1: 전체 빌드 확인**

```bash
make build
```

예상: 빌드 성공, 에러 없음.

- [ ] **Step 2: 전체 유닛 테스트**

```bash
go test ./internal/cluster/ ./internal/vfs/ -timeout=120s
```

예상: PASS.

- [ ] **Step 3: E2E 테스트 (기존 NFS/VFS 관련)**

```bash
make test-e2e 2>&1 | tail -30
```

예상: 기존 E2E 테스트 통과, 특히 NFS 읽기/쓰기 관련.

- [ ] **Step 4: 최종 커밋 (변경 없으면 스킵)**

모든 테스트 통과 확인 후 이미 커밋된 상태이면 이 단계는 건너뛴다.

---

## 구현 완료 체크리스트

- [ ] `BenchmarkECSplit` 추가 및 allocs/op 변경 없음 확인
- [ ] ECSplit double-append → copy 교체
- [ ] `grainFile.rc` 필드 추가
- [ ] 읽기 전용 Open → 스트리밍 모드 (rc 저장)
- [ ] `Read()` → rc 모드 직접 스트리밍
- [ ] `Seek()`, `ReadAt()` → rc 모드 시 loadExisting() fallback
- [ ] `Close()` → rc 미소비 시 rc.Close()
- [ ] `TestGrainFileStreamRead` PASS
- [ ] `TestGrainFileSeekFallback` PASS
- [ ] `TestGrainFileCloseWithUnconsumedRC` PASS
- [ ] 기존 VFS 테스트 전부 PASS (회귀 없음)
- [ ] `make build` 성공
